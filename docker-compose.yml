version: "3.9"

services:
  api:
    build: .
    container_name: ergonomia-api
    restart: always
    ports:
      - "8003:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
      - API_KEY=${API_KEY}
